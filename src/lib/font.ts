// Font CSS variables are defined statically in theme.css as system-font
// stacks (see --font-sans / --font-mono / --font-inter) rather than via
// next/font/google, since this dev environment's access to Google Fonts is
// unreliable and was repeatedly stalling/failing the dev server's first
// compile of any route.
export const fontVariables = '';
